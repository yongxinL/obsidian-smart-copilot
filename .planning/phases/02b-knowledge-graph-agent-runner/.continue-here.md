---
context: phase
phase: 02B-knowledge-graph-agent-runner
task: 0
total_tasks: 22
status: ready_to_execute
last_updated: 2026-05-16T07:07:15.645Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure or verified research.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Phase 2a dependency — Phase 2b CANNOT execute before Phase 2a. litellm is not installed in the dev environment. Phase 2a installs it as part of the LLM gateway. Running Phase 2b first will fail at import time.
- [ ] CONSTRAINT: Two human-verify gates — Plan 05 (OTel package legitimacy) and Plan 07 (eval quality gate) are `autonomous: false`. The executor MUST pause and wait for human confirmation at these checkpoints. Do NOT skip or auto-approve them.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Executing out of phase order | Running Phase 2b before Phase 2a leaves litellm uninstalled; all agent imports fail | blocking | Check Phase 2a status in STATE.md before starting execution; if not complete, run Phase 2a first |
| Skipping human-verify gates | Plan 05 and Plan 07 have checkpoint tasks requiring human sign-off; bypassing them risks installing unverified packages or advancing to Phase 3 with failing eval metrics | blocking | executor must pause on `checkpoint:human-verify` tasks and surface them to the user |

<current_state>
Phase 02B planning is fully complete. 7 PLAN.md files committed. No execution has started. The next action is to execute Phase 2b — but Phase 2a must be executed first.

Current project position:
- Phases 1a, 1b, 1c, 1d: complete
- Phase 2a: PLANNED (5 plans, 5 waves) — NOT YET EXECUTED
- Phase 2b: PLANNED (7 plans, 6 waves) — NOT YET EXECUTED
- Phases 3–8: not yet planned
</current_state>

<completed_work>
This session completed Phase 2b planning end-to-end:
- Research spawned → 02B-RESEARCH.md (54.7K) — schema gaps, wikilink extractor patterns, ReAct loop pitfalls, graph traversal CTE, eval strategy
- VALIDATION.md created — Nyquist test map for all 13 requirements with Wave 0 stubs
- PATTERNS.md created — 27 files classified, 22 analogs found in codebase
- 7 PLAN.md files created — all verified by plan-checker (0 blockers, 3 doc-level warnings)
- STATE.md updated — Phase 2b status set to "Ready to execute"
- ROADMAP.md annotated — plan list populated, wave structure documented
- All artifacts committed: `docs(02B): create phase plan — 7 plans, 6 waves, knowledge graph + ReAct agent + golden eval`
</completed_work>

<remaining_work>
**Phase 2b execution (7 plans, 6 waves):**

Wave 0 (parallel):
- 02B-01: Alembic migration 0006 — links nullable FK + mcp_mode enum fix + messages token/model + GoldenQuery fields + OTel package pins
- 02B-02: Test scaffolding — 12 stub test files + 16-page fixture vault + 25 golden queries + placeholders

Wave 1:
- 02B-03: Wikilink extractor + entity merge + timeline events + write-path hook in upsert_page

Wave 2:
- 02B-04: Recursive-CTE graph traversal service + brain.graph.traverse MCP tool (real impl)

Wave 3 (human gate before autonomous tasks):
- 02B-05: ReAct loop + 22-tool registry + BRAIN_FIRST system prompt + Phoenix/OpenTelemetry spans
  - Task 0: `checkpoint:human-verify` — verify OTel package legitimacy BEFORE continuing

Wave 4:
- 02B-06: agent_service (run_agent_query) + POST /api/v1/query REST endpoint + brain.query MCP tool + jobs.submit tool + MCP-REST parity

Wave 5 (human gate at end):
- 02B-07: Golden eval suite seed script + eval_agent.py (21 dimensions) + eval baseline
  - Task 3: `checkpoint:human-verify` — run eval, confirm Precision@5>=0.7 AND MRR>=0.6 BEFORE Phase 3

**Phase 2a must execute first** (LLM gateway + hybrid RAG pipeline — 5 plans).
</remaining_work>

<decisions_made>
- **Pure Python ReAct via LiteLLM tool_use** — No LangChain/CrewAI; zero-dependency loop; AI-SPEC locked this choice
- **PostgreSQL recursive CTE (CYCLE clause)** — No Apache AGE, no Neo4j; PostgreSQL 16 CYCLE clause eliminates manual visited-array tracking
- **links.dst_entity_id nullable** — Chosen over null-sentinel entity approach; CHECK constraint ensures either dst_entity_id or target_slug is set (D-04)
- **mcp_mode enum rename-and-recreate** — `disabled/client/server` → `disable/auto/manual`; uses alembic/0003 PostgreSQL pattern
- **Upsert on UNIQUE(src_page_id, target_text, link_type)** — ON CONFLICT DO UPDATE prevents duplicate links on re-save and handles concurrent watchdog+MCP write race
- **Wave 0 Nyquist stubs before implementation** — All test directories and stub files created in Wave 0 so subsequent waves have automated verification targets
</decisions_made>

<blockers>
- **litellm not installed** — Phase 2a execution resolves this (it installs litellm as LLM gateway infrastructure)
- **Phoenix sidecar unknown state** — Non-blocking; Plan 05 adds SMARTCOPILOT_TRACING_ENABLED env var guard for graceful fallback
- **OTel package human-verify gate** — Plan 05 Task 0 is a blocking human checkpoint before OTel packages are installed
</blockers>

## Required Reading (in order)
1. `.planning/phases/02B-knowledge-graph-agent-runner/02B-CONTEXT.md` — 16 locked decisions (D-01 to D-16); executor must know these before touching any code
2. `.planning/phases/02B-knowledge-graph-agent-runner/02B-AI-SPEC.md` — Full ReAct loop implementation (~265 lines locked), 22-tool registry spec, eval strategy
3. `.planning/phases/02B-knowledge-graph-agent-runner/02B-RESEARCH.md` — Schema gaps, pitfall register, architectural patterns
4. `.planning/phases/02B-knowledge-graph-agent-runner/02B-PATTERNS.md` — File analogs with concrete code excerpts; must be referenced in read_first for every new file
5. `.planning/phases/02A-llm-gateway-hybrid-rag-pipeline/` — Phase 2a plans (prerequisite — understand what infrastructure is being built there)

## Infrastructure State
- Docker container: not confirmed running (check with `docker ps | grep smart-copilot`)
- PostgreSQL: present in Docker; migration 0006 not yet applied (will be applied by Plan 02B-01)
- litellm: not installed (will be installed when Phase 2a executes)
- Phoenix/OpenTelemetry: unknown state (guard added in Plan 05)
- pgvector HNSW indexes: being created by Phase 2a (dependency)

## Pre-Execution Critique Required
N/A — planning complete and verified; no design-execution gap.

<context>
This session planned Phase 2b from scratch. The research surfaced four critical schema gaps in the existing codebase (migration 0006 required) and confirmed that the PostgreSQL CYCLE clause eliminates the need for any external graph engine. The AI-SPEC (77K) is the authoritative source for the ReAct loop implementation — the planner locked the exact implementation in Plan 05. Two human checkpoints protect against: (1) installing unverified packages, and (2) advancing to Phase 3 with a failing eval baseline. Phase 2a is the mandatory prerequisite — it wires litellm, pgvector HNSW, and BM25/RRF fusion that Phase 2b's agent depends on.
</context>

<next_action>
Start with: `/clear` then `/gsd:execute-phase 2a` (Phase 2a must run first). Once Phase 2a is complete, `/gsd:execute-phase 2b`.
</next_action>
