---
status: complete
phase: 01c-vault-watchdog-indexer
source: [01c-01-SUMMARY.md, 01c-02-SUMMARY.md, 01c-03-SUMMARY.md, 01c-04-SUMMARY.md, 01c-05-SUMMARY.md, 01c-06-SUMMARY.md, 01c-07-SUMMARY.md, 01c-08-SUMMARY.md]
started: 2026-05-11T08:30:00Z
updated: 2026-05-11T08:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Vault Path Confinement
expected: Writing a page places it at `/vaults/private/{username}/` under the correct user vault. Symlink escapes (`ln -s /etc/passwd ../etc.md`) and `..` traversal are rejected with `forbidden`. Cross-namespace access to `[[shared/Topic]]` resolves correctly.
result: pass
note: 10 tests passed — realpath on both vault_root and candidate, slug regex blocks traversal, namespace detection in parser.py

### 2. Compiled-Truth / Timeline Convention
expected: Writing a page with compiled-truth + timeline stores above-the-line content as rewritable summary and below-the-line as append-only event log. Page versions table records every change. Soft-deleted page has `deleted_at` + `delete_reason` set.
result: pass
note: 10 integration tests passed covering upsert_page, write_page, soft_delete_page, content-hash dedup, page versioning

### 3. Frontmatter Parsing + Content Hash
expected: Frontmatter parsed (`type: person`, `type: company`, `type: concept`, `type: idea`, etc.) and page type recognized. xxhash64 content-hash computed from raw file bytes. Re-index triggered only when hash changes (not on identical writes).
result: pass
note: 14 parser tests passed covering frontmatter, note types, wikilinks. xxhash64 content_hash used in services/pages.py with dedup check (skips DB write when unchanged)

### 4. Watchdog File Change Detection
expected: Watchdog detects a vault file modification within 1 second (inotify on Linux). Thread-to-asyncio handoff uses `asyncio.run_coroutine_threadsafe` exclusively (not `call_soon_threadsafe`). Reconciliation job corrects filesystem/database drift.
result: pass
note: 6 watcher tests + 3 reconcile tests passed. asyncio.run_coroutine_threadsafe verified (no call_soon_threadsafe found)

### 5. OpenAPI Spec Generation
expected: OpenAPI spec auto-generated at `/docs` and `docs/openapi.json` regenerated by pre-commit hook on route changes.
result: pass
note: 2 OpenAPI tests passed. docs/openapi.json exists with valid JSON. Pre-commit hook wired to regenerate on route changes.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]